# talkbot-openai
talkbot-openai is Siri like application with Open AI, and python.


Requirements:
- Python version >= 3.0 
- pip installed

To Run:
```
pip3 install -r requirements.txt
python3 speech.py
```
